import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './index.css'

// document.querySelector("main").addEventListener("click", (e) => console.log("click"));

ReactDOM.createRoot(document.getElementById('root')).render(
	// <React.StrictMode>
	<App />
	// </React.StrictMode>
)
